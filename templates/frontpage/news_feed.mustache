<section class="theme-eaduems-section theme-eaduems-news" id="{{sectionid}}" data-region="theme-eaduems-news-carousel">
    <div class="container-xl">
        {{> theme_eaduems/components/section_header }}
        <div class="theme-eaduems-card-surface">
            <div class="theme-eaduems-card-rows theme-eaduems-card-rows--news theme-eaduems-news__pages">
                {{#pages}}
                <div id="theme-eaduems-news-page-{{number}}" class="theme-eaduems-card-row theme-eaduems-card-row--news theme-eaduems-news__page" data-news-page="{{index}}" aria-hidden="{{#isactive}}false{{/isactive}}{{^isactive}}true{{/isactive}}"{{^isactive}} hidden{{/isactive}}>
                    {{#items}}
                        {{> theme_eaduems/components/news_card }}
                    {{/items}}
                </div>
                {{/pages}}
            </div>
            {{#hasmultiplepages}}
            <nav class="theme-eaduems-news__pagination" aria-label="Navegacao das noticias">
                <button class="theme-eaduems-news__arrow theme-eaduems-news__arrow--prev" type="button" data-action="show-previous-news-page" aria-label="Noticias anteriores" aria-controls="theme-eaduems-news-page-1" disabled>
                    <span aria-hidden="true">&#8249;</span>
                </button>
                <button class="theme-eaduems-news__arrow theme-eaduems-news__arrow--next" type="button" data-action="show-next-news-page" aria-label="Proximas noticias" aria-controls="theme-eaduems-news-page-1">
                    <span aria-hidden="true">&#8250;</span>
                </button>
            </nav>
            {{/hasmultiplepages}}
        </div>
    </div>
</section>

{{#js}}
require([], function() {
    document.querySelectorAll('[data-region="theme-eaduems-news-carousel"]').forEach(function(carousel) {
        if (carousel.dataset.newsCarouselReady === 'true') { return; }
        var pages = Array.prototype.slice.call(carousel.querySelectorAll('[data-news-page]'));
        var previous = carousel.querySelector('[data-action="show-previous-news-page"]');
        var next = carousel.querySelector('[data-action="show-next-news-page"]');
        var controls = [previous, next].filter(Boolean);
        var current = 0;
        var showPage = function(index) {
            current = Math.max(0, Math.min(index, pages.length - 1));
            pages.forEach(function(page, pageIndex) {
                var active = pageIndex === current;
                page.hidden = !active;
                page.setAttribute('aria-hidden', active ? 'false' : 'true');
            });
            if (previous) {
                previous.disabled = current === 0;
                previous.setAttribute('aria-controls', pages[current].id);
            }
            if (next) {
                next.disabled = current === pages.length - 1;
                next.setAttribute('aria-controls', pages[current].id);
            }
        };
        if (previous) {
            previous.addEventListener('click', function() { showPage(current - 1); });
        }
        if (next) {
            next.addEventListener('click', function() { showPage(current + 1); });
        }
        controls.forEach(function(control) {
            control.addEventListener('keydown', function(event) {
                if (event.key === 'ArrowRight') {
                    event.preventDefault(); showPage(current + 1); next && next.focus();
                } else if (event.key === 'ArrowLeft') {
                    event.preventDefault(); showPage(current - 1); previous && previous.focus();
                } else if (event.key === 'Home') {
                    event.preventDefault(); showPage(0); previous && previous.focus();
                } else if (event.key === 'End') {
                    event.preventDefault(); showPage(pages.length - 1); next && next.focus();
                }
            });
        });
        showPage(current);
        carousel.dataset.newsCarouselReady = 'true';
    });
});
{{/js}}